"""Local operator GUI for an aicash mint. Nothing here is protocol.

The modules in this package supervise and talk to a mint; they never
change how it behaves. See mintctl.py (process supervision), walletops.py
(wallet actions) and app.py (the loopback HTTP server and page).
"""
