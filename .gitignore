__pycache__/
*.pyc
.venv/
*.db
*.db-journal
# Windows download metadata, 101 files at import time
*:Zone.Identifier
# the mint's Ed25519 signing key - its identity. Never commit.
mint-keys.json
*-keys.json
*.log

# The operator GUI's workdir: a live mint ledger, its signing key, its
# operator credential, the supervision state and one sqlite file per
# wallet. Runtime state of one machine, and two of those files are
# secrets. The *.db / *-keys.json / *.log rules above miss
# mint-control.json, mint-token-digests.json and gui-state.json.
gui/var/
